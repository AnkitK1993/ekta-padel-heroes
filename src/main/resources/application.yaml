server:
  port: 8080
  servlet:
    context-path: /ekta-padel-heroes
spring:
  application:
    name: ekta-padel-heroes

  # Configuration for MySQL Database
  datasource:
    url: jdbc:mysql://localhost:3306/ektapadelheroes
    username: ankit
    password: ankit@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
